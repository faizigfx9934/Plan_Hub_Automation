# Copy this file to `.env` and fill in values for this specific laptop.
# Never commit the real .env — it is gitignored.

# PlanHub account credentials (one account per laptop, scoped to one US state)
PLANHUB_EMAIL=youraccount@example.com
PLANHUB_PASSWORD=yourpassword

# Laptop identity — used in logs and (future) telemetry
LAPTOP_ID=laptop-01
STATE=CA
