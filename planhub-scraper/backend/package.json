{
  "name": "planhub-telemetry",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute planhub-telemetry --remote --file=schema.sql",
    "db:local": "wrangler d1 execute planhub-telemetry --local --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
